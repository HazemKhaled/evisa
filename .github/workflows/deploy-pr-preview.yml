name: Deploy PR Preview

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    # github actions environment variables
    environment:
      name: pr-${{ github.event.pull_request.number }}
      url: https://pr-${{ github.event.pull_request.number }}.staging.gettravelvisa.com

    steps:
      - uses: actions/checkout@v5

      - name: Prepare Deployment Name
        run: |
          sed -i "s/__WORKER_ENVIRONMENT__/pr-${{ github.event.pull_request.number }}/g" wrangler.jsonc

      - name: 🤌 Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest
          run_install: true

      - name: Build
        run: pnpm exec opennextjs-cloudflare build
        env:
          NEXT_PUBLIC_GTM_ID: ${{ vars.NEXT_PUBLIC_GTM_ID }}

      - name: Build & Deploy Worker
        uses: cloudflare/wrangler-action@v3
        with:
          packageManager: pnpm
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
          environment: pr-${{ github.event.pull_request.number }}
          wranglerVersion: "4.33.1"
          secrets: |
            EXAMPLE_SECRET
          vars: |
            NEXT_PUBLIC_GTM_ID
        env:
          NEXT_PUBLIC_GTM_ID: ${{ vars.NEXT_PUBLIC_GTM_ID }}
          EXAMPLE_SECRET: "example value"
